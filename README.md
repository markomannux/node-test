# Node test #
## Uso ##
- eseguire npm install
- creare un file .env nella root con la chiave PORT=<*port_number*>
- eseguire npm start
- effettuando richieste a http://<*host*>:<*port_number*>/*requested_path* viene restituito *requested_path*
- effettuando una richiesta a http://<*host*>:<*port_number*>/killme il server viene terminato con errore
